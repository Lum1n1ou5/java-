/*
if-else
if-else 语句用于条件判断，可以用来手动检测某些错误情况，并根据条件执行不同的代码。这不是异常处理，而是预防错误发生。
*/

public class IfElseExample {
    public static void main(String[] args) {
        int num = 0;
        
        if (num == 0) {
            System.out.println("不能除以零！");
        } else {
            int result = 10 / num;
            System.out.println("结果: " + result);
        }
    }
}
